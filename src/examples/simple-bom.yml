description: >
  Generate a SBOM for targets

usage:
  version: 2.1
  orbs:
    scribe: scribe-security/orbs@1.2.3
  workflows:
    use-my-orb:
      jobs:
        - checkout
        - scribe/install
        # Bom checkout
        - scribe/bom:
            target: dir:.
            verbose: 2
        # Bom some image
        - scribe/bom:
            target: alpine:latest
            verbose: 2
